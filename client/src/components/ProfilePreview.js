import React from 'react';

import '../stylesheets/ProfilePreview.css';

const ProfilePreview = () => {
  return (
    <div>Profile preview stuff goes here.</div>
  );
};

export default ProfilePreview;